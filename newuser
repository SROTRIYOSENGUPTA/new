import java.sql.Connection;
import java.util.Properties;

public class YourClassName {
    
    // Assuming statementHash is a class-level HashMap
    private static HashMap<String, Object> statementHash = new HashMap<>();

    // Assuming log is a class-level Logger
    private static Logger log = Logger.getLogger(YourClassName.class.getName());

    public static void main(String[] args) {
        // Create dummy instances of required objects (Connection, SBeanMetaUtil, and SBeanMeta)
        Connection conn = null;
        SBeanMetaUtil sBeanMetaUtil = new SBeanMetaUtil();
        SBeanMeta sBeanMeta = new SBeanMeta();

        try {
            Properties props = getnewSearchStatement(conn, 1, sBeanMetaUtil, sBeanMeta);

            // Iterate through and print out each property
            for (String name : props.stringPropertyNames()) {
                String value = props.getProperty(name);
                System.out.println(name + ": " + value);
            }
        } catch (Exception e) {
            e.printStackTrace();
        }
    }

    public static Properties getnewSearchStatement(Connection conn, int searchId, SBeanMetaUtil sBeanMetaUtil, SBeanMeta sBeanMeta)
            throws Exception {
        Properties props = new Properties();

        // ... (the rest of your existing method code)
        
        return props;
    }

    // Include the classes SBeanMetaUtil, SBeanMeta, etc., here or in separate files.
}
